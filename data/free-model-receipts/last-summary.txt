We repaired the free‑model lane that was completely down—its startup script crashed and the API key was wrong—by fixing the script’s failure, adding proper authentication, correcting a directory‑filter bug, and now 645 free models respond to calls (e.g., a chat request returns in 0.85 seconds) at $0 cost.
