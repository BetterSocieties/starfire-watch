We fixed the free‑model pipeline (cards 3123, 4504, 3527) by correcting the startup script so it no longer aborts when no gateway is listening, replacing the placeholder “omniroute” with a real API key and enforcing proper authentication, adding a health‑check that requires an actual model list, and tightening the directory‑sweep to exclude only node_modules and .git—now free models are reachable again and return real, $0 responses.
