The free model lane was down in the cloud because the start‑up script died when it could not find a listener and the inference server rejected the placeholder key, but after fixes the gateway now checks for a real model list, works with a proper API key, and returns free answers in under a second, showing 645 models available.
