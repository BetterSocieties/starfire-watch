An update fixed the customer qualifier flow to match the live page, restored database read-retries to prevent silent failures, routed secret vault requests through the standard reader, and logged new system findings regarding unmonitored writes and mailbox permissions.
