We’ve released a new background‑knockout feature for BR‑Product that automatically strips backgrounds, runs a separate nudity check (blocking any of 5 disallowed classes above 0.35) while requiring a manual adult‑content attestation, verifies that the PNG’s alpha channel truly contains transparency, stores the pass/fail verdict as a single text field, and now logs exact runtime costs (about 68 seconds cold start and <1 second warm).
