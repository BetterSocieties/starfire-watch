On September 12 2026 the free‑model lane failed in every cloud run — its start‑up script quit early with zero bytes on stdout and exit 1 because it could not get a listener PID, the gateway returned HTTP 530 showing no healthy connector, authentication was rejected when callers sent the bearer “omniroute”, the health check now required a real model list before reporting success, the /v1/chat/completions endpoint was left unprotected while /v1/models demanded auth, the model‑name sweep mistakenly skipped entire “archive” and “evidence” folders, and duplicate card naming (e.g., card 4218) appeared — until each issue was fixed the lane fell back to paid usage.
