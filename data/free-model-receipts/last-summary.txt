We fixed the cold‑lane sending system so it’s properly resumed, repaired a bug where the pause flag didn’t match the “restricted to verified” verdict, added clear names for the three possible outcomes, corrected a strict validation error that was blocking all sends, fixed a hidden deactivated sender that silently prevented deliveries, and rolled back a script that was overwriting settings and blocking repo writes.
