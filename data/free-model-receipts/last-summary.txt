After fixing the start‑up script that quit when no listener was found, correcting missing authentication, adding a health check that requires a live model list, closing an unguarded quota‑spending route, and widening the model‑name sweep to include only node_modules and .git, the free lane now reports 645 models, returns a free chat completion in 0.847 seconds at zero cost, and shows 1 341 open cards with a clean model list of 621 strings.
