We’ve launched a new background‑knockout pipeline for product images that automatically blocks nudity (using NudeNet), requires a manual adult‑content attestation instead of an unreliable age check, verifies that the PNG’s alpha channel truly contains transparency (rejecting fully opaque outputs), and now runs end‑to‑end with clear pass/fail exit codes (cold start ~68 seconds, warm < 1 second) while measuring its own cost.
