Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-08-28T19:05-19:45Z three-role-loop-20260828T1905Z

Cloud fire. One row worked to done, ISSUE-2654, and it is the row that was stopping every
other cloud fire from taking any row at all.

- **A cloud fire could rank the board and could not claim anything on it.** The 18:34Z fire
  fixed `scripts/prioritize.py` so a machine with no gh can still READ the tracker, and left
  the identical defect standing in `scripts/claim-issue.py`, which the three-role procedure
  makes every cloud fire run twice: step 2 to claim before any work, step 6 to close with
  evidence. Read live 19:14Z, `which gh` empty and a claim died
  `FileNotFoundError: [Errno 2] No such file or directory: 'gh'` inside `read_card`. The
  procedure says never work an unclaimed row, so the honest reading is that every cloud fire
  since the tracker migration was blocked at step 2 and only did not notice.
- **The write cannot come from inside a python process either**, re-measured this fire:
  api.github.com answers HTTP 403 to a script with and without GITHUB_TOKEN, while git push
  and the session's own GitHub tools both work. So `--card-json` takes the card the fire
  already fetched, runs the SAME decision code the gh path runs, and prints the comment for
  the fire to post rather than posting it. Nothing about the gh path changed and
  `--self-test` still exits 0.
- **The gate is `scripts/check-the-claim-works-without-gh.py`** and it decides real claims
  rather than grepping for an option: 10 assertions with the card handed in and PATH set to
  /nonexistent, exit 0; `--inverted` withholds the card, all 10 fail on the new error that
  names `--card-json`, exit 1. Proven on the live card too: this fire WON 2654 exit 0 and a
  session calling itself `some-other-fire` was told LOST exit 3, off the real thread read
  with the session's GitHub tools.
- **The free lane built it and could not be shipped as written.** `scripts/free-agent.sh` on
  nemotron-3-ultra-550b-a55b returned the whole change in one pass
  (`evidence/free-agent/claimer-without-gh-20260828T1925Z/`). Three defects, all fixed by the
  fire: it deleted 12 blocks of the incident-history comments this repo keeps on purpose, it
  added a `_CARD_JSON` global that is assigned and never read, and its `--inverted` run
  withheld nothing. It re-ran the held-card case the positive run already covers and returned
  1 because that case correctly failed, which is precisely the always-passes check the
  procedure warns about.
- **`free-agent.sh` refused to run at all until it was fixed**, and that is worth its own
  line because it sends mechanical work back to the paid model silently.
  `cloud-fire-setup.sh` installs opencode under `$HOME/.npm-global`, nothing puts that on
  PATH, and the script believed the lane was uninstalled. It now looks where the setup
  actually put it. Measured: `command -v opencode` empty with a fire's real PATH, and
  `/root/.npm-global/bin/opencode` after the fix.
- **Skipped and why:** the ranked top row was ISSUE-1623