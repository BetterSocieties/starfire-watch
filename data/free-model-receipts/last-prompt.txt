Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-08-28T12:21-13:27Z infra-pulse-20260828T1221Z

Infrastructure-only fire. 6 board rows worked to their ceiling, 4 new rows filed, and one
live money defect found and corrected on a customer-facing page.

- **schoolofplay.org went from 1 page to 4** (row c3a5b2c6): home, apply, about, contact,
  plus a sitemap the site never had. `/apply` carries the 3 LIVE Stripe deposit buttons and
  the `sop-discovery-call` booking embed. One writer, `scripts/sop-build-site.py`, which
  edits index.html in place only for the nav, the dashes and the deposit figures so the live
  waitlist form is never rewritten. All 4 pages and the sitemap answer 200 and
  POST /api/waitlist answers 400, not 405, so the server side code survived.
- **THE MONEY DEFECT.** The page advertised deposits of JPY 300,000 / 400,000 / 500,000
  while the 3 live payment links charge JPY 100,000 / 100,000 / 200,000, read from
  acct_1TRkZTAVdrFDHPJ7, all 3 active. The page now quotes the button everywhere. Decision
  and what would reverse it: STATE/AUTONOMY-DECISIONS.md 2026-08-28. The page also claimed
  the payment links were not live yet; they have been for weeks.
- **The publish guard was crying wolf on every site that has server side code**
  (row THE-PUBLISH-GUARD-CALLS-A-PAGES-FUNCTION-STALE...). It asked the live site for its
  Pages function SOURCE, which Cloudflare never serves, so `deploy-cf-pages.sh` published
  twice and exited 1 on deploys that were perfectly fine. Fixed, both halves of its DONE-TEST
  run live. A SECOND defect in the same file, found on the same publish: the email
  obfuscation folding missed a mailto carrying a subject and a body, and the anchor form of
  the obfuscation. After both fixes school-of-play publishes at exit 0.
- **The 9 unresolvable cold emails, decided** (row 4e9d7c13): option 3, they stay in the
  follow-up population and stay outside every rate reading, the word is `unproven`. Written
  up in AUTONOMY-DECISIONS. The gate's allowance of 9 was forgiving a population its own
  query can no longer reach while letting 9 fresh ones through; set to 0.
- **Background sessions now launch on a 5KB context, not 27KB** (row e30ef262): CLAUDE-BG.md,
  `scripts/spawn-bg-session.sh` (per-session worktree, relative symlink, refuses rather than
  falling back), and a pre-commit gate. 81.9 percent cut.
- **Echo Studio errors and omissions incident log** (row 9a4e6c8b): the source was never a
  missing portal form, it is `echo_engagements.revision_requests`. Record half proven live
  by id; the alert half is refused because no client record exists to link to, and the
  script exits 5 rather than pretending Adrien was told.
- **Echo Studio LinkedIn draft skill** (row 5e0c2a4f): drafts on the free lane, and the brand
  audit is a GATE, so a failing draft is never written. Proven live by id, then cleaned up.
- **USPTO has no keyless machine route any more and TESS is retired** (row 5af63e9d): every
  route probed live answers 401 or 403. `scripts/br-trademark-watch.py` refuses with exit 3
  and names the free registration URL rather than scraping.
- Row 5e1c9a71 was tagged hands=cloud while its own body says the cloud cannot do it. Retagged