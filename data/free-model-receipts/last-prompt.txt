Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-09-04 profit-pulse-A 20:13Z, the cold lane is switched on in both of the ways it needs to be

Card 3444 closed: the OpsJuice cold lane is RESUMED onto the 70 mailbox-verified named
addresses. Card 3539 had moved the verdict to `RESTRICTED_TO_VERIFIED` at 19:48Z and correctly
left `send_paused` alone, so for 25 minutes the gate said the lane may send and the lane was
paused. `check-the-cold-lane-pause-flag-matches-the-verdict.py` caught it at exit 1 and named
its own repair. A verdict is not a switch. Applied by the gate on its own verdict, state event
`0f5cd656`, `send_paused` read back false by hand, both guards exit 0 after. Commit `29df678e`.

Card 3537 closed: the three outcomes leaving the cold sender's no-send branch now name
themselves, so `lastNodeExecuted` says which one happened without pulling the whole run. The
first attempt used `typeValidation: strict`, copied from the workflow's own existing switch, and
strict THROWS on an item missing a referenced field rather than failing to match it. That would
have failed every run of the branch, including a healthy full sending day. Caught before it
reached anything scheduled: execution `111681`, error, no item reached any terminal. Commit
`e62468e3`.

Also repaired, and the most dangerous thing found today: a routine write left the sender itself
`active: false`, HTTP 200 and no warning. The lane had been un-paused 19 minutes earlier, so it
was correctly configured to send and physically could not, and a deactivated sender produces no
failure to alarm on. Nothing in the check registry watches that flag.

Also reverted, not committed: `cloud-fire-setup.sh` overwrites the committed
`.claude/settings.json` with a blocking gate dispatcher that then refuses the fire's own repo
writes, which is the machinery removed twice on 1 August for the same reason.

- Corrections made in-fire rather than edited away: a claim that the gate's window had aged out
  from 84 to 39, when card 3539 had narrowed it (`35764144`).