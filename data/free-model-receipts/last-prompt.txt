Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-08-22T12:26Z three-role-loop -- row A-RETURNING-SCORECARD-OR-CALCULATOR-LEAD-IS-ANSWERED-500

- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: anybody who used the OpsJuice scorecard or the missed-call calculator a SECOND time was answered `db write failed` with status 500 on the money page. prospects carries a unique index on the address, and 2 of the 3 lead handlers in the live payment webhook were plain inserts, so the second arrival was refused by the database. It had been invisible because a different capitalisation dodges the index and quietly made a second row instead, which is the fault the sibling row fixed an hour earlier; fixing that fold is what exposed this one. Both handlers now fold the address and upsert on it, `?on_conflict=email` with `Prefer: resolution=merge-duplicates,return=representation`, the same write the automation platform makes. Deployed 47812 bytes at 12:20:55Z, 9 bindings and compatibility_date 2026-01-01 unchanged. Proved live rather than reasoned: the same address posted twice to each endpoint answered 200 both times and returned the SAME row id, and each address read back exactly 1 row while its raw capitalised form read back 0.
- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: the fix created a second fault on its way in and it was caught before the deploy by reading the schema rather than assuming it. A merge-duplicates upsert is ON CONFLICT DO UPDATE over every column the payload names, so a field sent at its empty value writes that emptiness over the returning person's row. The scorecard form has no company field and the calculator asks for 4 numbers, and both were sending `business_name: body.business_name || ''` and `email_source: null`. handleAssessmentLead had been carrying the same 2 empty fields since its own upsert went live at 11:46Z. All 3 handlers now leave first_name, business_name and email_source out of the payload and add them only when they have a value. Proved in the same probe: the second scorecard post carried no company at all and the row still reads the company the first post gave.
- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: the schema was read from the store's own OpenAPI definition, GET /rest/v1/ with Accept: application/openapi+json, which is the documented way to ask and settles what a bare insert may leave out. prospects requires id, niche, business_name, status, created_at, updated_at and outreach_state; business_name defaults to the empty string, status to 'pending', outreach_state to 'queued'; first_name and email_source are nullable with no default. niche is the not-null column a bare insert trips on.
- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: the daily gate that went green over this the whole time now judges all 3 prospects writers instead of 1, and names the handler in every fault it reports. It also gained a rule refusing an empty-valued field in the written payload, brace-matched to the payload itself so the opt-in record and the forward to the automation platform are not read as the write. Self-test 20 checks 0 failed, and the run before the deploy reported 11 faults across the 3 handlers, which is the red-before proof. Live run after the deploy exit 0, 1533 distinct addresses in the store and none of them held twice. Money path re-read afterwards: receiver root 200, all 3 lead routes 400 without an address, both Stripe routes 403 on an unsigned body. Probe rows and their 4 opt-in events deleted, 204, read back empty.
- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: THE FREE BUILDER LANE WORKED ON A FAITHFUL REWRITE, which is the shape it lost 6 attempts on earlier today. Manager exit 0, PASS from the clean-context judge on attempt 3, 3 free builder calls and 3 paid judge calls. What changed was the task file rather than the model: the source of both functions was handed to it inside the requirements, and every edit was named as a checkable statement. The accepted answer was then diffed against the original by hand rather than taken on the judge's word, 15 changed lines, exactly the 6 intended edits and nothing else.
- 2026-08-22T12:26Z three-role-loop-20260822T1209Z: 2 rows opened off live reads and deliberately not folded into this one. Neither handler sets outreach_state, so the column default 'queued' files a hand raiser in the cold stranger pool, which is the exact fault handleAssessmentLead's own comment says it was given 'inbound' to avoid. And all 3 handlers hard-code status 'pending' inside a merging upsert, so a returning person is moved back to pending whatever they had become.