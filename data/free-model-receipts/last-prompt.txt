Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-08-27T18:5xZ three-role-loop -- row CHECK-RUNNER-ANSWERED-ONLY-231, the money band no longer spends the whole run before the dead-sender check is asked

`scripts/fire-checks.py` answered 9 of 231 due checks inside its 100 second budget and left
`check-the-cold-sender-actually-sent-this-week` unasked, which is the instrument that catches a
dead cold sender. Root cause read live off `STATE/fire-checks-last-run.json` rather than argued:
`MONEY_PATH` is both the priority order AND the wall-clock exemption, and the band's own recorded
cost was 166.6 seconds against a 100 second budget, so the band alone spent the allowance and all
222 ordinary checks behind it were refused with `not_started_why=budget-spent`. The check that
went unasked costs 1.2 seconds.

Landed: that check joins `MONEY_PATH`; the band is ordered cheapest first inside itself by
`expected_cost`; a new `ORDINARY_FLOOR_SECONDS = 20` gives the ordinary list a deadline of
`max(budget, money band spend + floor)` so an overrunning band can no longer leave it zero, while a
cheap band changes nothing; and `check-stripe-events-are-being-delivered`, 73.6 seconds and 44
percent of the band, moves to `cadence_hours` 6 with its own `cadence_why`. The self-test rule that
money names stay hourly was amended rather than deleted: outside the hourly band is allowed only
with a `cadence_why`, and a new assertion fails by name if the dead-sender check leaves `MONEY_PATH`.

Proof, both directions. Real: `--only check-the-cold-sender-actually-sent-this-week` printed
`ok (1.6s)` and `ran 1 of 1 due, 281 in registry (33 hourly, 213 daily, 25 weekly)`, exit 0;
`--self-test` reads 92 checks, 0 failed, exit 0, which also clears the pre-existing red
`hourly set costs less than its budget 479s of a 420s budget`. Inverted: remove the name from
`MONEY_PATH` and the self-test exits 1 naming it. Unasked-on-3-or-more-fires fell 196 to 171.
Commit 76bb6765. Free-lane receipt `evidence/free-agent/check-runner-answered-only-231/`; three
defects in what it returned were fixed by hand and the registry half it skipped was written here.