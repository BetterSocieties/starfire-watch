Summarize the newest changelog entry below in 1 plain-English sentence a non-technical founder understands. Use digits for numbers. Some details have been replaced with a description of what they were; summarise what is there and never invent the removed value. Entry:
## 2026-09-12 the free model lane was dead in every cloud fire and said nothing, cards 3123, 4504 and 3527, 22:00Z

- The lane was dead for two separate reasons stacked on each other, which is why several earlier passes each fixed something real and it still did not work. `scripts/start-free-gateway.sh` runs under `set -euo pipefail`, and line 98 assigned from a `listener_pid()` whose `lsof` exits 1 when nothing is listening, so the script terminated on that assignment before ever reaching the passthrough or install blocks below it. Read live: zero bytes on stdout, zero on stderr, exit 1, and a `bash -x` trace ending at `+ EXISTING_PID=`. It could only appear to work where a gateway was already listening, which is the laptop. Meanwhile the hosted address answered HTTP 530 with body `error code: 1033`, which Cloudflare documents as a tunnel with no healthy connector, meaning the laptop was off. So both routes to a free model were out at once and every cloud fire told to use the free lane absorbed the work into paid turns instead.
- Second fault, the 401 card 3123 was opened on: OmniRoute 3.8.50 requires authentication on the inference plane and the callers presented the bearer literal `omniroute`, which the server rejects. The admin plane had been taught to log in long ago and the inference plane never was. A real key is minted with `POST /api/keys`, whose live validator wants `name` and rejects the `label` its own OpenAPI example shows.
- The start-up now also refuses to report success on process state alone: it requires a real model list back before it says anything worked, which is the Google SRE rule that you health check the signal the caller depends on. That refusal earned its keep within the hour by exposing the 401 instead of hiding it.
- Auditor own reads after the change, in a cloud container with the laptop offline: the gateway reports 645 models and exits 0, a free chat completion answered in 0.847 seconds, `llm-router.py --task code` returned a real answer from a free model at 0 dollars, and `free-agent.sh` reached a free model with real tool calls in its transcript.
- Card 4504, filed and closed the same fire: `POST /v1/chat/completions` enforced no auth at all while `/v1/models` did, on a gateway bound to 0.0.0.0, so the route that spends provider quota was the unguarded one. Bound loopback only, decision and rejected alternative recorded in `STATE/AUTONOMY-DECISIONS.md`. The free lane config carried `omniroute` as its apiKey, written before any key exists, and worked only because of that unguarded route; it now receives the real key and `the-free-lane-never-carries-the-omniroute-literal` refuses the literal coming back. The new check first shipped with a key-shaped self-test fixture and `.githooks/pre-commit` refused the commit; the gate was right, so the fixture changed rather than the gate being bypassed.
- Card 3527, separate: `EXCLUDE_DIR_NAMES` in the model-name sweep skipped `archive` and `evidence` wholesale, so a live caller under either name was never swept and the gate reported green over ground it had not covered. Now skips only `node_modules` and `.git`. Proved by plant and catch on all three escapes the card named. Clean tree afterwards: 621 strings, exit 0; self-test 58 checks with the same 4 environment-only failures and no new one.
- `STATE/board-snapshot.json` refreshed off the live tracker: 1341 open rows, 0 severed yaml blocks. Found while doing it, and confirming what another session recorded the same hour: `next-card.py` does not skip a card another live session holds, so it named 4218 twice, before and after `claim-issue.py` answered LOST on it.
- North star unchanged and read live at 20:56:17Z: 0 dollars a month, gap 20000 dollars.